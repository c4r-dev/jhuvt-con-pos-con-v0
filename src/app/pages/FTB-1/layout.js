export const metadata = {
  title: "Fix That Bias",
  description: "Interactive activity to identify and address methodological concerns in research studies",
};

export default function FTB1Layout({ children }) {
  return children;
}