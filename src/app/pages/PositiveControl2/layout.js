export const metadata = {
  title: "Positive Controls",
  description: "Positive Controls activity for validating research methods",
};

export default function PositiveControl2Layout({ children }) {
  return children;
}