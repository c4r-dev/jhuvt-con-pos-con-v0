export const metadata = {
  title: "Fix That Bias - Results",
  description: "Results from the Fix That Bias activity showing your performance",
};

export default function FTBResultsLayout({ children }) {
  return children;
}